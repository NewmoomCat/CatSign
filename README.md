# CatSign 签到插件

### v0.2.0

### 签到指令: "/sign"

### 加入自动签到

#### config.json :
```json
{
  "money": "50", // 签到金额
  "title": "签到插件", //插件前缀
  "auto-sign": false //自动签到, 要不要启用由腐竹决定
}
```

### 本插件分为两个版本 (UME 和 JSONE)
### 请根据你服务器所使用的经济系统选择
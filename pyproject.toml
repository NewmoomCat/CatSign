[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "endstone-catsign"
version = "0.1.0"
description = "Endstone首款签到"

[project.entry-points."endstone"]
catsign = "endstone_catsign:CatSign"